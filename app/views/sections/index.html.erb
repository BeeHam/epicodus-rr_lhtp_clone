<h1>Sections</h1>

<ul>
  <% @sections.each do |section| %>
    <li><%= section.name %>
      <ul>
        <% section.lessons.each do |lesson| %>
          <li><%= lesson.name %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
